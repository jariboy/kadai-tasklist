<div style="text-align:center;">
  <h1>タスク編集フォーム</h1>
  <sec>
  <%= form_for(@task) do |f| %>
  <%= f.label :content,"タスク" %>
  <%= f.text_field :content %>
  <%= f.submit "投稿" %>
  <%end%>
  <%= link_to "タスク一覧に戻る", tasks_path%>
</div>